# Techfy Image Tools

This repository contains a privacy-first set of client-side image tools (resize, crop, compress) inspired by modern web image utilities. Tools run in the browser; no uploads for client-side features.

Run locally:

1. npm install
2. npm run dev

Build:

npm run build
